#include "RosbeeProtocolHandler.h"

int getTargetLocation(uint8_t message) {
	//xLocation = message.payload << 4 ;
	//yLocation = message.Payload << 8;
	//return sqrt((xLocation^2) + (yLocation^2));
    return 0;
}
